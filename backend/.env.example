# Copiar este archivo a .env.development (local) o .env.production (producción)
# y rellenar con los valores reales. NUNCA subir los archivos .env reales a git.

JWT_SECRET=cambia_esto_por_un_secreto_seguro
JWT_EXPIRATION=1d

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=tu_usuario_db
DB_PASSWORD=tu_password_db
DB_NAME=conejo_motors_dev

# Credenciales del usuario administrador inicial (solo se usa si no hay usuarios en la BD)
ADMIN_EMAIL=admin@conejomotors.com
ADMIN_PASSWORD=cambia_esto_en_produccion
