# docker-compose (base de datos local de desarrollo).
# Copiá este archivo a ".env" (que NO se versiona) y poné una contraseña propia:
#   cp .env.example .env
POSTGRES_PASSWORD=cambia-esta-contrasena-local
